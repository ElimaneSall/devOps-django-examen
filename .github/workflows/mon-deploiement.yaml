apiVersion: apps/v1
kind: Deployment
metadata:
	name: my-deployment
spec:
	replicas: 3
	selector:
		matchLabels:
			app: monprojet
	template:
		metadata:
			labels:
				app: monprojet
		spec:
			containers:
			- name: "my-app-container"
				image: "my-app-image"